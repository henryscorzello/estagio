@model IEnumerable<Now.Alunos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Turmas.Turmas1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Turmas.Turmas1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Aluno }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Aluno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Aluno })
        </td>
    </tr>
}

</table>
